# subchi

Subdomain checker

positional arguments:
domain the domain to check

options:
-h, --help show this help message and exit
-o OUTPUT_FILE output file
-s SUBDOMAINS_FILE subdomains file
-c COMPARE_FILE compare file
